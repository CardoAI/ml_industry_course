# Task

Using the *same portfolio data provided* and the models you fitted in the previous tasks, generate a cash-flow simulation that combines:

1. the dependence structure from your copula model(s), and  
2. the default/prepayment dynamics from your survival model(s).

Use:

- the provided portfolio (exposures, contractual terms, rates, maturities, recovery/LGD assumptions if given),
- your selected copula model (including fitted parameters),
- your selected survival model (including fitted parameters and covariates, if any).